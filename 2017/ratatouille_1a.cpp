#include <iostream>

int main() {
	int T;
	scanf("%d", &T);

	for (int i = 0; i < T; i++) {
		int N, P;
		scanf("%d %d", &N, &P);

		int weights[50];
		for (int j = 0; j < N; j++) {
			scanf("%lf", &weights[j]);
		}

		for (int j = 0; j < N; j++) {
			int packages[50];
			for (int k = 0; k < P; k++) {
				int package;
				scanf("%d", &package);
				
			}
			
		}

	}
}